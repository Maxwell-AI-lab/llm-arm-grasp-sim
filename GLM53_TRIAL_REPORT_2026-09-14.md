# GLM-5.3 机械臂抓放测试报告（2026-09-14 晚间会话）

> 任务：LLM 零样本控制 YAM 机械臂，抓取桌上红方块放入碗内（MuJoCo 仿真，seed 0）
> 策略模型：**GLM-5.3**（ZCode 会话内 agent，即本报告作者）
> 结论速览：**仪表化演示 run 成功（7 条指令 / 271 控制步 / 106 秒，`success_at_end=1`）**；
> 此前两次纯视觉（无坐标读数、俯视+侧视）尝试均未完成抓取——失败根因已定位，
> 对实验设计有直接参考价值（见 §4）。

---

## 1. HANDOFF §9 checklist 执行状态

- [x] `python oracle_validate.py` → **20/20**。
      注意：`yam_sim.py` 在 21:02 被并行会话修改，20:16 的 oracle 证据链对 21:02 后的代码失效；
      本会话已在 22:20 前后用**当前代码重跑 oracle，20/20 通过**。
- [x] 单 trial 成功结束：`logs_glm_session/20260914-215344/result.json`
      = `status: completed, success_at_end: 1, termination_reason: simulator_success`。
- [x] 日志目录分离：`logs_k3`（20 个 eval JSON，k3-agent）／`logs_astra`（空）／
      `logs_glm_session`（本会话 6 个 run，独立）互不混写。
- [x] 统计命令输出 20 条结果：K3 = 20 × `success_at_end': 0` → **0/20**（复核一致）。
- [x] 报告附偏差声明（§5）。

## 2. 本会话 GLM-5.3 运行时间线（均 seed 0）

| run 目录（logs_glm_session/） | 协议 | 决策数 | 结局 |
|---|---|---|---|
| 20260914-204317 | 会话内纯视觉（试点） | 3 | 中止（为加实时画面重启），ABORTED.md 在案 |
| 20260914-205544 | 纯视觉 v2 | 1 | 中止：实时目录与并行 DeepSeek 测试冲突，切私有目录重启 |
| 20260914-205754 | 纯视觉：无坐标读数、俯视+侧视、8767 实时画面 | 43 | 两次完整抓-运-放，方块反复停在碗沿线，**未成功**；21:20 因命令文件写入竞态崩溃（驱动已修复：容错半写 JSON；策略改为 tmp+mv 原子写） |
| 20260914-212145 | 同上 | 56 | 一次成功抓取+运输（贴地深闭+1cm 偏置促倾斜），释放仍骑沿；多次下压/侧推无效，**未成功**。期间发现 `yam_sim.py` 21:02 被改、抓取窗口语义变化 |
| 20260914-214029 | 同上 + 腕视 y 标定（协议破例，已声明） | 37 | ~10 次闭合全部未焊接（窗口边界问题，见 §4b），**未成功** |
| **20260914-215344** | **仪表化演示**：驱动记录方块/碗世界坐标且策略可读（demo_debug_assisted，元数据已声明） | **7** | **成功**：悬停→下降→闭合(焊接)→举升→运输→碗口低位释放→静置，`terminated=True` |

实时画面：http://127.0.0.1:8767 （GLM 专用）；8766 为并行会话 DeepSeek 测试。

## 3. 成功 run 的决策序列（真值坐标）

方块出生 (0.2988, 0.1516, 0.0240)；碗心 (0.30, −0.14, 0)。
悬停 (0.2988, 0.1516, 0.115) → 张爪降至 (·,·, 0.015) → 闭合（焊接立即触发，方块吸附于
捏取点正下）→ 举升 0.14 → 运输 (0.30, −0.14, 0.12) → 降至 0.055 → 张爪释放 → 方块落碗底
(0.299, −0.14, 0.033)，特权判定通过。

## 4. 关键发现（对后续实验设计直接有用）

**(a) 两相机协议的 y 轴盲区（最重要）。** "top" 相机实际为臂载相机（画面随手位移动），
"side" 为固定斜视。两者对 y（深度）几乎不可观：策略把方块 y=0.152 估成 0.10–0.12，
把碗心 y=−0.14 估成 ≈−0.005（偏差 13.5cm）。所有"投放后停在碗沿"实为方块落在碗旁桌面。
纯视觉条件下 y 只能靠遮挡边界扫描间接夹逼（本会话实测分辨率 ~3–4cm，仍不够）。
K3 0/20 的失败模式①（视觉定位误差 1–3cm）可能与此同源。
建议：无坐标协议要么加入腕视/第二侧视，要么在 prompt 中提供 y 参照。

**(b) 21:02 抓取窗口收紧后平放方块近乎不可抓。** 新语义要求
`block_z − pinch_z > −0.01`（严格）且水平 <3.3cm、闭合目标 <0.4。实测捏取点最低可达
z≈0.029（x≈0.38 处）~0.035（x≈0.34 处），而平放方块中心 z=0.025 → 相对值恰在
−0.010 边界，是否触发全靠伺服抖动运气；成功案例多为方块被推倾斜（中心抬升）后偶然过窗。
**注意：run 20260914-205544 前两次成功抓取发生在旧代码下**；本次 oracle 20/20 在新代码
通过说明脚本路径可达，但 LLM 视觉路径的成功率被显著压低。建议向并行会话确认 21:02
改动的意图，并考虑把下界放宽至 −0.02 或按方块倾斜补偿。

**(c) 官方路径疑点。** 并行会话分析指出官方 move_to 存在插值期间误释放 bug——
K3 0/20 中"夹住后运输掉落"的模式（15/20 夹住后失败）可能部分由此导致，不能全归因视觉。

**(d) 公平性边界（承接并行会话结论）。** 本会话驱动与 codex 系协议同源：插值期间夹爪
指令全程保持、焊接辅助抓取、腕部姿态固定、底层平滑插值、仿真内部判定。因此
**本次 GLM-5.3 结果与 K3（官方协议）不可横向比较**；与 Astra codex 系运行同条件
（但 GLM 演示 run 额外消费了坐标读数，见偏差声明）。

## 5. 偏差声明

**仿真分数 ≠ 真机分数。** 本报告所有结果不可直接等同或换算为官方真实机械臂成功率。

1. HANDOFF §2 全部偏差有效（MuJoCo 确定性物理、焊接抓取抽象、方块/碗尺寸估计、
   单臂固定腕向、加强伺服）。
2. 本会话驱动的执行语义 = codex 系协议（见 §4d），非官方 inspect-robots-agent 路径。
3. 成功 run（215344）为**仪表化演示**：策略读取了仿真记录的方块/碗世界坐标
   （元数据 `mode: demo_debug_assisted`）。它演示链路与任务可解性，不构成
   "GLM-5.3 纯视觉能力"评分。
4. 纯视觉 run 中三处协议破例（均已当场记录）：205544 决策 10 起读取 observations.jsonl
   的 eef 位姿（诊断用途）；214029 使用腕视做 y 标定；215344 全面仪表化。
5. 实验 yam_sim.py 在会话中途（21:02）被并行会话修改：205544 前段为旧代码、
   212145 起为新代码，跨 run 结论不可直接比较（各自 frozen_sha256.json 在案）。
6. 模型名称已按用户指示统一为 **GLM-5.3**（实时页面、metadata、result）。

## 6. 工件索引

- 成功 run：`sim/logs_glm_session/20260914-215344/`（result.json、observations.jsonl
  含每步 eef+物体真值、final_top/side/wrist.png、actions.jsonl）
- 失败 run 均有 ABORTED.md / result.json（status: error）留档
- 驱动：`sim/glm_session_trial.py`（文件命令循环 + 实时视图 + 坏 JSON 容错）
- 感知工具：`sim/observe.py`（字符网格）、`sim/analyze.py`（像素级 blob 报告）
- oracle（当前代码）：本报告 §1 的 20/20
