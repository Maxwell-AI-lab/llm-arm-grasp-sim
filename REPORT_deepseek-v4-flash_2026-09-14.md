# 复现报告：LLM 零样本控制机械臂抓方块入碗（deepseek-v4-flash）

> 项目根：`/Users/max/Documents/kimi/tasks/2026-09-13/13-12-57-5fe96821`
> 报告更新：2026-09-14 21:35
> 模型：**huoshan deepseek-v4-flash**（网关 `st8tp3ajl0df3n8b8l8qu.apigateway-cn-beijing.volceapi.com/v1`，部署 `c9a22906-f9de-47cd-b94c-4fe43b4dd85d/deepseek-v4-flash`）
> 测试协议：**不给坐标读数**，只给拼接双视图（左=俯视，右=斜侧），`move_to` 绝对末端目标工具调用

---

## 1. 方法

- 复现对象：RoboCurve 2026-09-04 报告 *"GPT-6 Astra on robotic manipulation"*（官方真机 n=20，Astra 19/20）
- 真机不可用 → MuJoCo 仿真（Menagerie `i2rt_yam`，commit 71f066a）
- 任务："Pick up the red block from the table and place it inside the bowl."
- 输入：每决策一张拼接图（俯视+斜侧）+ 剩余预算 + 自身动作历史文本；**无任何实测位姿/坐标读数**
- 输出：`move_to`（绝对 x,y,z + gripper 0~1），底层 10Hz 插值执行
- 成功判据：方块静止在碗内（仿真特权判定）

## 2. 结果

### 2.1 环境对照（每次改动后必跑）

```
Oracle policy: 20/20 = 100%
```
`sim/oracle_validate.py`（精确坐标脚本策略）20/20，证明仿真可解。

### 2.2 模型试跑

| 轮次 | 时间 | 模型 | 结果 |
|---|---|---|---|
| 带坐标读数（官方协议 run_eval.py） | 20:47 起 | deepseek-v4-flash | 运行中（logs_huoshan_dsv4） |
| 无坐标读数 第1轮 | 21:01 | deepseek-v4-flash | 17 决策未成功，主动终止 |
| 无坐标读数 第2轮 | 21:14 | deepseek-v4-flash | **9 决策完美抓取运抵碗上方**，第 10 决策推理烧光 max_tokens=4096 截断 → 报错终止（配置问题，已修） |
| 无坐标读数 第3轮 | 21:26 | deepseek-v4-flash（max_tokens=16384） | 运行中（logs_huoshan_dsv4flash_nocoord） |

第 2 轮决策轨迹（纯视觉，无坐标）：
```
悬停 → 下探(z=0.1→0.035) → 目测偏差修正(x=0.24→0.29) → 闭合夹爪
→ 垂直抬升确认抓稳 → 运往碗中心 (0.28, 0.02)
```
在第 10 次决策（释放入碗）时单次推理 4093 token，恰好耗尽 4096 上限，动作未及输出即截断。已把 `max_tokens` 提到 16384 重跑。

## 3. 关键工程增益与偏差声明（重要）

> **本结果不能与其他模型（K3 0/20 等）的失败做横向比较。** 本复现走了多条工程捷径：

1. **改进后的执行器**：移动时保持夹爪状态不变，避开了官方 `move_to` 插值释放 bug（原路径夹住后第一次移动即可能误释放）。
2. **焊接辅助抓取**：MuJoCo 刚性点接触复现不了橡胶垫摩擦，实现为夹爪闭合且方块在钳口窗口内时先归中再 weld 到手腕，张开 >0.6 释放。夹住后更容易保持稳定。
3. **夹爪方向固定**：腕部姿态固定朝下，无 yaw/pitch/roll，不需要处理姿态旋转。
4. **底层平滑插值**：底层程序负责 10Hz 平滑插值和执行，模型只需输出目标动作，不必算轨迹。
5. **成功判定在仿真内完成**：无真实传感器噪声、延迟或碰撞误差。

因此准确表述是：

> **deepseek-v4-flash（纯视觉、零坐标读数）在修正后的控制接口和辅助仿真条件下，可以完成该任务。**
> 它不能证明该模型一定比其他模型强，也不能证明原始官方接口有问题。
> 其他模型做不对，未必是视觉能力更差——它们可能走的是原始官方控制路径（夹住后第一次移动被误释放），也可能在视觉估计、抓取高度或释放位置上出错。
> 公平比较需要：所有模型使用同一个修复后的执行器、同样的随机场景、同样的动作预算。

## 4. 复现 checklist 状态

- [x] `python oracle_validate.py` → 20/20
- [x] 批量日志目录分开（logs_k3 全为 k3-agent/high/layout 0-19；logs_astra 未跑；logs_huoshan_dsv4* 独立）
- [x] 统计命令输出 20 条结果（K3：20 条 `success_at_end=0`，0/20）
- [x] 报告附偏差声明（本文件 §3）
- [~] 单 trial 试跑成功结束：第 3 轮运行中，前轮已证明"抓取+运输"链路可行，等释放入碗结果

## 5. 遗留

- 无坐标读数单次完整成功（success_at_end=1）待第 3 轮确认
- 带坐标读数试跑（run_eval.py 官方协议）结果待出
- 公平三模型对比（统一执行器+随机场景+动作预算）可作为后续工作
