# Checklist 执行记录（2026-09-14）

项目：`/Users/max/Documents/kimi/tasks/2026-09-13/13-12-57-5fe96821`

已先完整阅读 `HANDOFF.md`。本次未修改仿真、策略或既有评测日志，未调用模型 API。

## 执行状态

- [x] 本次重新运行 `sim/oracle_validate.py`：seed 0–19 全部 SUCCESS，最终输出 `Oracle policy: 20/20 = 100%`，进程退出码 0。
- [x] 核验既有单 trial 完整结束：20 个 JSON 的 `status` 均为 `success`，均存在 `results.metrics.success_at_end`；对应的 20 个 `k3_final_<seed>.out` 也有完成状态和结果。
- [ ] 本次新单 trial 试跑：尚未启动。当前任务进程没有 `OPENAI_API_KEY`、`KIMI_API_KEY` 或 `KIMI_BASE_URL`，且 `sim/.env` 不存在。交接文档中“环境变量已存在”的说明不适用于当前任务环境。密钥值未被打印。
- [x] 核验正式日志不混模型：`sim/logs_k3` 的 20 个 eval JSON 均为 `k3-agent`、`effort=high`，seed 恰为 0–19，无重复和缺失。Astra 尚无结果，不计为零分；其日志必须写入单独的 `sim/logs_astra`。
- [x] 统计并逐条检查 20 条结果：20 个完成 trial，0 个 errored trial，`success_at_end` 全部为 0，任务成功数 **0/20**。
- [x] 本记录附实验偏差声明。

`status: success` 只表示评测流程正常完成；抓取成功与否由 `success_at_end` 判断。这两个字段不可混淆。

## 核验范围与限制

本次 oracle 的 20/20 表明在当前实现与 20 个既定种子下，拥有精确位置的脚本策略可完成任务；它不证明仿真与真机等价，也不证明不存在其他仿真缺陷。

K3 结果来自既有正式 JSON 与 transcript 的核验，不是本次重新跑出的模型结果。交接文档中的官方模型成绩、模型可用性和价格，本次未进行外部核实，不作为本记录的新结论。

## 实验偏差声明

**仿真分数 ≠ 真机分数。** 当前仿真结果不可直接等同或换算成官方真实机械臂成功率；也不能表述为与官方实验“方法论完全一致”。

1. 使用 MuJoCo 仿真，物理具有确定性且无传感噪声，与真实硬件不同。
2. 抓取保持使用归中与焊接抽象：进入钳口触发窗口并闭合后，先向中心归拢，再 weld 到手腕；张开夹爪后释放。这不是仅靠真实接触摩擦的抓取。
3. 方块边长估为 5 cm；碗由 10 段盒体近似组成，内径约 11 cm，缺乏精确原始尺寸。
4. 使用单臂，腕部固定朝下，没有 yaw/pitch/roll 控制；原实验硬件与动作空间不同。
5. 位置伺服增益被加强，以降低仿真重力下垂；控制动力学与原硬件不同。

## 下一步

在本机配置可用的模型凭据后，指定使用哪个模型/端点。无需在聊天中粘贴密钥。

新试跑应使用新的独立日志目录，不覆盖 K3 的正式 20 条结果。按 HANDOFF 先运行 1–2 次，确认链路、完成状态、任务得分和真实单次成本，再继续批量评测。当前缺少凭据，因此没有发起新的单次或批量模型调用。

## 附：晚间会话补充（2026-09-14 ~22:30）

- oracle 在 21:02 版代码上重跑：20/20（此前 20:16 的证据链因 yam_sim.py 中途被改而失效）。
- GLM-5.3 会话内测试完成：纯视觉 2 次尝试未成功（根因定位：y 轴盲区 + 抓取窗口边界），
  仪表化演示 run 成功（7 指令 / 271 步 / success_at_end=1）。
- 详细记录：`GLM53_TRIAL_REPORT_2026-09-14.md`。
